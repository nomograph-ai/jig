# gitlab.com/nomograph/jig .gitlab-ci.yml
#
# Pipeline structure provided by nomograph/pipeline tool-rust@v4.1.2.

include:
  - template: Jobs/Secret-Detection.gitlab-ci.yml
  - component: gitlab.com/nomograph/pipeline/tool-rust@v4.1.2
    inputs:
      binary_name: jig
      project_description: "Agent-shape testing harness: runtime-in-the-loop batteries, LLM-as-judge scoring."
      crates_publish: true
      in_kit_registry: false

check-symlinks:
  stage: lint
  image: alpine:3.19
  before_script:
    - apk add --no-cache bash git
  script:
    - bash scripts/check-symlinks.sh
